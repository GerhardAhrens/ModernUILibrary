<UserControl
    x:Class="ModernUIDemo.MyControls.ChartControlsUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ModernUI="clr-namespace:ModernIU.Controls;assembly=ModernUILibrary"
    xmlns:ModernUIChart="clr-namespace:ModernIU.Controls.Chart;assembly=ModernUILibrary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="ChartControls"
    d:DesignHeight="450"
    d:DesignWidth="900"
    mc:Ignorable="d">

    <UserControl.Resources>
        <RadialGradientBrush x:Key="ChartBackground">
            <RadialGradientBrush.RelativeTransform>
                <TransformGroup>
                    <ScaleTransform CenterX="0.5" CenterY="0.5" ScaleX="1.15" ScaleY="1.15" />
                    <TranslateTransform X="-0.08" Y="-0.08" />
                </TransformGroup>
            </RadialGradientBrush.RelativeTransform>
            <GradientStop Offset="1" Color="White" />
            <GradientStop Offset="0" Color="WhiteSmoke" />
        </RadialGradientBrush>

        <LinearGradientBrush x:Key="IsMouseOver" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Offset="0" Color="#FFDC9314" />
            <GradientStop Offset="0.564" Color="#FFD6CA79" />
            <GradientStop Offset="0.982" Color="#FFE6A03A" />
        </LinearGradientBrush>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="14" Text="Beispiel für verschiedene Chart Typen" />
        <Separator Height="3" Background="Green" />

        <TabControl Grid.Row="1" SelectedIndex="0">
            <TabItem Header="Barchart" IsSelected="True">
                <Grid x:Name="gridBarChart" Background="White">
                    <ModernUIChart:BarChart
                        x:Name="chartTest"
                        Margin="0,0"
                        Caption="ModernIU - BarChart"
                        ChartBackground="{StaticResource ResourceKey=ChartBackground}"
                        ChartColorMouseOver="{StaticResource ResourceKey=IsMouseOver}"
                        ChartItemWidth="20"
                        ChartLegend="{Binding Path=BarLegend, ElementName=ChartControls}"
                        ChartMargin="5,0,0,0"
                        ChartScrollBarVisibility="Visible"
                        ItemSource="{Binding Path=BarRows, ElementName=ChartControls}"
                        MouseLeftButtonDown="OnMouseLeftButtonDown" />
                </Grid>
            </TabItem>

            <TabItem Header="Piechart">
                <Grid x:Name="gridPieChart">
                    <ModernUIChart:PieChart
                        x:Name="pieChart"
                        Margin="0,0"
                        Caption="ModernIU - PieChart"
                        ChartBackground="{StaticResource ChartBackground}"
                        ChartColorMouseOver="{StaticResource IsMouseOver}"
                        ChartLegend="{Binding Path=PieLegend, ElementName=ChartControls}"
                        DataContext="{Binding Path=PieRows, ElementName=ChartControls}" />
                </Grid>
            </TabItem>

            <TabItem Header="Stackedchart" IsSelected="False">
                <Grid>
                    <ModernUIChart:StackedChart
                        x:Name="stackedChart"
                        Margin="10"
                        Caption="ModernIU - StackedChart"
                        ChartBackground="{StaticResource ChartBackground}"
                        ChartColorMouseOver="{StaticResource IsMouseOver}"
                        ChartCornerRadius="1"
                        ChartItemWidth="20"
                        ChartLegend="{Binding Path=StackedLegend, ElementName=ChartControls}"
                        ChartMargin="5"
                        ChartScrollBarVisibility="Visible"
                        ChartShowCaption="Visible"
                        DataContext="{Binding Path=StackedRows, ElementName=ChartControls}" />
                </Grid>
            </TabItem>

            <TabItem Header="Treemap" IsSelected="False">
                <Grid>
                    <ModernUIChart:RegionTreeMapsPanel x:Name="treemapDemo" Grid.Column="0">
                        <Rectangle
                            x:Name="recPanel1"
                            ModernUIChart:TreeMapsPanel.Weight="{Binding Path=Panel1.FillWeight, UpdateSourceTrigger=PropertyChanged}"
                            Fill="{Binding Path=Panel1.FillColor, UpdateSourceTrigger=PropertyChanged}"
                            ToolTip="{Binding Path=Panel1.FillTooltip, UpdateSourceTrigger=PropertyChanged}" />

                        <Rectangle
                            x:Name="recPanel2"
                            ModernUIChart:TreeMapsPanel.Weight="{Binding Path=Panel2.FillWeight, UpdateSourceTrigger=PropertyChanged}"
                            Fill="{Binding Path=Panel2.FillColor, UpdateSourceTrigger=PropertyChanged}"
                            ToolTip="{Binding Path=Panel2.FillTooltip, UpdateSourceTrigger=PropertyChanged}" />

                        <Rectangle
                            x:Name="recPanel3"
                            ModernUIChart:TreeMapsPanel.Weight="{Binding Path=Panel3.FillWeight, UpdateSourceTrigger=PropertyChanged}"
                            Fill="{Binding Path=Panel3.FillColor, UpdateSourceTrigger=PropertyChanged}"
                            ToolTip="{Binding Path=Panel3.FillTooltip, UpdateSourceTrigger=PropertyChanged}" />

                        <Rectangle
                            x:Name="recPanel4"
                            ModernUIChart:TreeMapsPanel.Weight="{Binding Path=Panel4.FillWeight, UpdateSourceTrigger=PropertyChanged}"
                            Fill="{Binding Path=Panel4.FillColor, UpdateSourceTrigger=PropertyChanged}"
                            ToolTip="{Binding Path=Panel4.FillTooltip, UpdateSourceTrigger=PropertyChanged}" />

                        <Rectangle
                            x:Name="recPanel5"
                            ModernUIChart:TreeMapsPanel.Weight="{Binding Path=Panel5.FillWeight, UpdateSourceTrigger=PropertyChanged}"
                            Fill="{Binding Path=Panel5.FillColor, UpdateSourceTrigger=PropertyChanged}"
                            ToolTip="{Binding Path=Panel5.FillTooltip, UpdateSourceTrigger=PropertyChanged}" />
                    </ModernUIChart:RegionTreeMapsPanel>
                </Grid>
            </TabItem>
        </TabControl>

    </Grid>
</UserControl>
